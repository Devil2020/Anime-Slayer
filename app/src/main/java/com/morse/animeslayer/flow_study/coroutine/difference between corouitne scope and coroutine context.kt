package com.morse.animeslayer.flow_study

import kotlinx.coroutines.coroutineScope
import kotlinx.coroutines.runBlocking

fun main ()= runBlocking {

    coroutineScope {

    }

}

/*   Coroutine Scope it is an interface already have only one object and it is coroutine Context Only */

/*   Each CoroutineScope have inside it default CoroutineContext and Job inside it */